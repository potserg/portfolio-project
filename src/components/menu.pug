mixin nav(className, items, activeItem)
  nav(class=`${className}`)
    ul(class=`${className}__list`)
      each item, ndx in items
        li(class=`${className}__item`)
          a(class=`${className}__link` href=`#${item.anchor}`)= item.name