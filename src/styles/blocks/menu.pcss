.menu__list {
  display: flex;
  justify-content: flex-start;
  flex-wrap: wrap;
}

.menu__item {
  cursor: pointer;
  margin-right: 30px;
  padding: 5px;
  &:last-child {
    margin-left: 0;
  }
  &:hover {
    border-bottom: 2px solid $links-color;
  }
}

.menu__link {
  color: $links-color;
}